---
- name: open http port
  firewalld: service=http permanent=true state=enabled immediate=yes zone=public
  tags: nginx

- name: open https port
  firewalld: service=https permanent=true state=enabled immediate=yes zone=public
  tags: nginx
